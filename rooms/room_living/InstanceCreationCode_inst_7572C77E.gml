/*if(global.game_stage > 2) {
	//remove_available_location(RoomChoices.APARTMENTS, Where.INNER,RoomChoices.BEDROOM_LUIGI);
	//ds_list_add(contextMenuList, ContextMenuItemType.INSPECT)
	global.dead = true;
	
} */
x_original = 620;
if(global.game_stage == 3) {
	x = 200
	visible = false;
} 
